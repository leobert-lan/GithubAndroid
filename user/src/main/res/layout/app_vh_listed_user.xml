<?xml version="1.0" encoding="UTF-8" ?>
<layout>

    <data>

        <variable
            name="vo"
            type="osp.leobert.android.github.user.widget.ListedUserVO2" />

        <variable
            name="itemInteract"
            type="osp.leobert.android.github.user.widget.ListedUserItemInteract" />

        <variable
            name="vh"
            type="osp.leobert.android.github.base.recyclerview.AbsViewHolder" />
    </data>

    <FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:layout_marginStart="15dp"
        android:layout_marginEnd="15dp">

        <com.google.android.material.imageview.ShapeableImageView
            imageUrl="@{vo.avatar}"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:layout_gravity="center_vertical" />


        <!-- android:onClick="@{(view)->itemInteract.foo(vh.getAdapterPosition(),vo)}"-->
        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="center_vertical"
            android:layout_marginStart="50dp"
            android:ellipsize="end"
            android:singleLine="true"
            android:text="@{vo.name}" />
    </FrameLayout>
</layout>